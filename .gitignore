node_modules/

# Anchored to the repository root so they cannot match source directories such
# as src/core/coverage.
/dist/
/out/
/coverage/

.env
.env.local
*.log
.DS_Store

# local dev data (mongod started outside docker)
/.devdata/
